{
  "name": "camera-pc-control-center",
  "version": "0.1.0",
  "private": true,
  "description": "Web canvas/dashboard to view and control authorized Windows PCs through a VPS gateway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ws": "^8.18.3"
  }
}
